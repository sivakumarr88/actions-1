name: My First Workflow

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v6
            
            - name: Greet, List files & Read files
              run: |
                echo "My first GitHub Action workflow"
                ls -la
                cat README.md